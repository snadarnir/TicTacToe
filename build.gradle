apply plugin: 'java'
apply plugin: 'application'

mainClassName = "is.ru.TicTacToe.TicTacToeMain"

repositories {
    jcenter()
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.5'
    testCompile 'junit:junit:4.11'
}

task stage {
	dependsOn installDist
}